# 소스는 UTF-8 BOM. Git 이 내용을 건드리지 않게 한다.
*.h    -text
*.cpp  -text

# CSV 는 런타임 파서가 첫 글자를 본다. 변형되면 안 된다.
*.csv  -text

# 네비 데이터는 바이너리
*.bin  binary

# 외부 라이브러리는 받은 그대로 둔다. 줄바꿈을 바꾸면 upstream 과 대조할 수 없다.
ThirdParty/** -text
